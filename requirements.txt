# Makerspace RAG - Dependencies
# Install with: pip install -r requirements.txt

# Web Framework
flask>=2.3.0
flask-login>=0.6.0
flask-sqlalchemy>=3.1.0  # Database ORM
werkzeug>=2.3.0

# Database
pymysql>=1.1.0           # MariaDB/MySQL driver
cryptography>=42.0.0     # For MySQL auth

# LLM Integration
ollama>=0.1.0
requests>=2.28.0

# Search & ML
scikit-learn>=1.2.0
numpy>=1.24.0

# Document Processing - PDF (in order of preference)
pymupdf4llm>=0.0.5      # Best for RAG - extracts markdown, handles tables
pdfplumber>=0.10.0      # Fallback - good table extraction
PyPDF2>=3.0.0           # Basic extraction

# OCR for scanned/image-based PDFs
easyocr>=1.7.0          # Pure Python OCR - no Tesseract needed
pdf2image>=1.16.0       # PDF to image conversion

# Excel/XLSX processing
openpyxl>=3.1.0         # Read/write Excel files

# Optional: For semantic embeddings (simple_rag.py)
# torch>=2.0.0
